class BaseConverter(BaseConverter):
       def __call__(self, value):
           try:
               return super().__call__(value)
               raise BadMethodCallException from ex
           except BadMethodCallException as e:
               raise BadMethodCall fromex_valid_valid_username_username_valid
           except BadMethodCallException:
              raise BadMethodCall from
           except BadMethod:valid:
               raise
       def __invalid:.username.username:<<username from.username
valid_valid_valid__username_username
username
<username
username
e"`username:username.username.username username<usernameusername.username username_username<username
username.username.username